class Foo = thread Bar {
  var f : int
}
thread trait Bar {
  require var f : int
}
let x = new Foo(f:5) in 
  let y = x in 
  finish{
    
    async{
      x.f 
    }
    async{
      y.f (* cannot read thread alias in different thread*)
    }
  } ;
  x.f
  end
end