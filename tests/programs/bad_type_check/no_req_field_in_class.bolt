class Foo = read Bar { 
   const g : int
  (* No field f in class definition even though required by 
     the trait Bar that Foo is using *)
}
read trait Bar {
  require const f : int
}
let x = new Foo() in 
  x
end